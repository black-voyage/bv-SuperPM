.DS_Store

# Secrets — NEVER commit. Keys live outside the repo / in env vars.
*.local.json
*.local.md
*service-account*.json
*-adminsdk-*.json
bv-second-brain-*.json
serviceAccount*.json
.env
.env.*
chat-proxy/.env
ingest/_catalog-prepurge-backup.json
node_modules/
ingest/node_modules/

# Local seeded preview/screenshot scratch files — never commit or deploy
_preview_*
