{
  "name": "bv-superpm-chat-proxy",
  "version": "1.0.0",
  "description": "Claude chat proxy for BV SuperPM (holds the Anthropic API key server-side)",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=18" },
  "dependencies": { "express": "^4.19.2" }
}
